---
type Props = {
  category: string;
};

const { category } = Astro.props;
---

<a
  class="text-teal-900 rounded-3xl uppercase text-xl border-2 border-teal-900 whitespace-nowrap p-2"
  href={`/category/${category}`}>
  {category}
</a>
